# HUMANIZATION.md - Требования к человекоподобности бота

> **КРИТИЧЕСКИ ВАЖНО**: Человекоподобность - ключевой аспект проекта. Цель - показать CCP как боты имитируют живых игроков.

---

## Зачем нужна хуманизация?

### Цель проекта

Мы **хотим**, чтобы CCP научилась детектировать ботов. Для этого наш бот должен:
1. Работать как реальный бот (использовать те же методы)
2. Имитировать человека (как делают настоящие ботоводы)
3. Показать CCP **что именно** нужно детектировать

### Что детектируют анти-бот системы?

- ⚠️ Телепортация курсора (мгновенные прыжки)
- ⚠️ Идеальные тайминги (всегда одинаковые задержки)
- ⚠️ Повторяющиеся паттерны (одинаковые последовательности)
- ⚠️ Неестественная точность (попадание ровно в центр пикселя)

---

## Движения мыши

### ❌ НЕПРАВИЛЬНО: Телепортация

```python
# БОТ БУДЕТ ДЕТЕКТИРОВАН
import pyautogui

# Курсор мгновенно прыгает в точку (x, y)
pyautogui.click(500, 300)
```

**Проблемы:**
- Мгновенное перемещение курсора
- Нулевое время перемещения
- Невозможно для человека
- Легко детектируется

### ✅ ПРАВИЛЬНО: Плавное движение

```python
# ХУМАНИЗИРОВАННЫЙ БОТ
from eve.mouse import move_to, click
import random
import time

# Плавное движение курсора к цели
move_to(500, 300, duration=random.uniform(0.3, 0.7))

# Пауза перед кликом (человек не кликает мгновенно)
time.sleep(random.uniform(0.05, 0.15))

# Клик
click(500, 300)
```

**Преимущества:**
- Видимое движение курсора
- Рандомизированная скорость
- Пауза перед кликом
- Похоже на человека

### Реализация в eve/mouse.py

```python
def move_to(x: int, y: int, duration: float = 0.2):
    """
    Переместить курсор в указанную позицию плавно.

    Args:
        x, y: Координаты назначения
        duration: Время перемещения (секунды)
                 Рекомендуется: 0.3-0.7 для хуманизации
    """
    pyautogui.moveTo(x, y, duration=duration)
```

### Использование в скриптах

```python
# Всегда используй move_to перед click
def click_element(x, y):
    """Кликнуть по элементу с хуманизацией."""
    # Плавное движение
    move_to(x, y, duration=random.uniform(0.3, 0.7))

    # Пауза перед кликом
    time.sleep(random.uniform(0.1, 0.3))

    # Клик
    click(x, y)

    # Пауза после клика
    time.sleep(random.uniform(0.1, 0.2))
```

---

## Рандомизация таймингов

### ❌ НЕПРАВИЛЬНО: Фиксированные задержки

```python
# БОТ БУДЕТ ДЕТЕКТИРОВАН
time.sleep(1.0)  # Всегда ровно 1 секунда
time.sleep(1.0)  # Всегда ровно 1 секунда
time.sleep(1.0)  # Идеальный паттерн - детектируется
```

**Проблемы:**
- Идеальная точность
- Повторяющийся паттерн
- Нечеловеческая предсказуемость

### ✅ ПРАВИЛЬНО: Рандомизированные задержки

```python
# ХУМАНИЗИРОВАННЫЙ БОТ
import random
import time

def humanized_delay(min_sec: float = 0.3, max_sec: float = 0.8):
    """Человекоподобная задержка."""
    delay = random.uniform(min_sec, max_sec)
    time.sleep(delay)

# Использование
humanized_delay(0.5, 1.5)  # Задержка от 0.5 до 1.5 секунд
humanized_delay(0.3, 0.8)  # Более короткая задержка
humanized_delay(1.0, 3.0)  # Более длинная задержка
```

### Когда использовать задержки

1. **Между действиями:**
   ```python
   click_on_image("button1.png")
   humanized_delay(0.3, 0.7)  # Пауза перед следующим действием
   click_on_image("button2.png")
   ```

2. **Перед кликом:**
   ```python
   move_to(x, y, duration=0.5)
   humanized_delay(0.1, 0.3)  # Пауза перед кликом
   click(x, y)
   ```

3. **Ожидание загрузки:**
   ```python
   click_on_image("open_menu.png")
   humanized_delay(0.5, 1.0)  # Ждем открытия меню
   ```

4. **После завершения действия:**
   ```python
   click_on_image("confirm.png")
   humanized_delay(0.2, 0.5)  # Пауза после подтверждения
   ```

---

## Параметры хуманизации

### Скорость движения мыши

```python
# Быстрое движение (но не телепортация)
move_to(x, y, duration=random.uniform(0.2, 0.4))

# Нормальное движение (рекомендуется)
move_to(x, y, duration=random.uniform(0.3, 0.7))

# Медленное движение (осторожность)
move_to(x, y, duration=random.uniform(0.5, 1.0))
```

**Рекомендация**: Используй `0.3-0.7` для большинства случаев.

### Задержки между действиями

```python
# Очень короткая пауза (рефлексивное действие)
humanized_delay(0.1, 0.2)

# Короткая пауза (быстрая реакция)
humanized_delay(0.2, 0.5)

# Средняя пауза (обычное действие) - рекомендуется
humanized_delay(0.5, 1.5)

# Длинная пауза (размышление, чтение)
humanized_delay(1.0, 3.0)
```

### Время реакции

```python
def wait_for_element_and_react(template: str):
    """Ждать появления элемента и реагировать с задержкой."""

    # Ждем элемент
    coords = wait_image(template, timeout=30)
    if not coords:
        return False

    # ВАЖНО: Человек не реагирует мгновенно
    # Добавляем время реакции
    reaction_time = random.uniform(0.3, 0.8)
    time.sleep(reaction_time)

    # Плавно двигаем курсор
    x, y = coords
    move_to(x, y, duration=random.uniform(0.3, 0.7))

    # Пауза перед кликом
    humanized_delay(0.1, 0.3)

    # Клик
    click(x, y)

    return True
```

---

## Шаблон хуманизированной функции

```python
def humanized_click_on_image(
    template: str,
    timeout: float = 10,
    confidence: float = 0.9,
    reaction_time: tuple = (0.3, 0.8),
    move_duration: tuple = (0.3, 0.7),
    pre_click_delay: tuple = (0.1, 0.3),
    post_click_delay: tuple = (0.1, 0.2)
) -> bool:
    """
    Найти изображение и кликнуть с полной хуманизацией.

    Args:
        template: Путь к шаблону
        timeout: Таймаут ожидания
        confidence: Порог совпадения
        reaction_time: Диапазон времени реакции (мин, макс)
        move_duration: Диапазон времени движения мыши
        pre_click_delay: Диапазон задержки перед кликом
        post_click_delay: Диапазон задержки после клика

    Returns:
        bool: True если успешно, False если не найдено
    """
    logger.debug(f"Поиск и клик: {template}")

    # Ждем появления элемента
    coords = wait_image(template, timeout=timeout, confidence=confidence)
    if not coords:
        logger.warning(f"Элемент не найден: {template}")
        return False

    x, y = coords

    # Время реакции (человек не реагирует мгновенно)
    time.sleep(random.uniform(*reaction_time))

    # Плавное движение к элементу
    move_to(x, y, duration=random.uniform(*move_duration))

    # Пауза перед кликом
    time.sleep(random.uniform(*pre_click_delay))

    # Клик
    click(x, y)

    # Пауза после клика
    time.sleep(random.uniform(*post_click_delay))

    logger.debug(f"Клик выполнен: {template}")
    return True
```

### Использование

```python
# С параметрами по умолчанию (рекомендуется)
humanized_click_on_image("eve_button.png")

# С кастомными параметрами
humanized_click_on_image(
    "eve_important_button.png",
    timeout=15,
    reaction_time=(0.5, 1.2),  # Более медленная реакция
    move_duration=(0.5, 1.0)    # Более медленное движение
)
```

---

## ⚠️ Что НЕ делать

### ❌ НЕ добавляй случайные ошибки

```python
# НЕ ДЕЛАЙ ТАК
def click_with_miss():
    # Промах курсором - может сломать бота
    miss_x = x + random.randint(-30, 30)
    move_to(miss_x, miss_y)  # ❌ ОПАСНО
```

**Почему нет:**
- Может сломать логику бота
- Бот должен быть эффективным
- Промахи не критичны для исследования

### ❌ НЕ добавляй рандомные действия без разрешения

```python
# НЕ ДЕЛАЙ ТАК автоматически
if random.random() < 0.1:
    check_chat()  # ❌ Может сломать флоу
    move_camera()  # ❌ Может сбить таргет
```

**Почему нет:**
- Может сломать последовательность действий
- Непредсказуемое поведение
- Трудно отлаживать

**Когда можно:**
- Пользователь **явно попросил** добавить рандомные действия
- Спросил пользователя и получил одобрение
- Точно известно что это безопасно для данного скрипта

---

## Checklist хуманизации

Перед отправкой скрипта на тестирование, проверь:

- [ ] Используется `move_to()` перед **каждым** `click()`
- [ ] Все задержки рандомизированы (нет `time.sleep(1.0)`)
- [ ] Добавлены паузы перед и после кликов
- [ ] Есть время реакции после `wait_image()`
- [ ] Duration в `move_to()` - рандомизированная
- [ ] Логируются все действия
- [ ] НЕТ случайных ошибок/промахов
- [ ] НЕТ рандомных действий (если не одобрено юзером)

---

## Примеры из реального кода

### Пример 1: Простой клик

```python
# ❌ ПЛОХО
click_on_image("button.png")

# ✅ ХОРОШО
logger.info("Кликаю на кнопку")
coords = find_image("button.png")
if coords:
    x, y = coords
    time.sleep(random.uniform(0.2, 0.5))  # Время реакции
    move_to(x, y, duration=random.uniform(0.3, 0.7))
    time.sleep(random.uniform(0.1, 0.3))
    click(x, y)
    time.sleep(random.uniform(0.1, 0.2))
    logger.info("Клик выполнен")
```

### Пример 2: Последовательность действий

```python
def open_menu_and_select_option():
    """Открыть меню и выбрать опцию."""

    # Шаг 1: Открыть меню
    logger.info("Открываю меню")
    if not humanized_click_on_image("menu_button.png"):
        return False

    # Пауза для открытия меню
    humanized_delay(0.3, 0.8)

    # Шаг 2: Выбрать опцию
    logger.info("Выбираю опцию")
    if not humanized_click_on_image("menu_option.png"):
        return False

    # Пауза после выбора
    humanized_delay(0.2, 0.5)

    logger.info("Опция выбрана")
    return True
```

---

## Итог

**Золотое правило**: Если действие выглядит слишком идеально - добавь рандомизации.

**Помни**: Цель - показать CCP как боты пытаются выглядеть как люди через простую хуманизацию (плавные движения + рандомные задержки).

**Всегда хуманизируй**:
- ✅ Движения мыши (move_to + duration)
- ✅ Задержки (random.uniform)
- ✅ Время реакции (после wait_image)

**НЕ добавляй**:
- ❌ Случайные ошибки (может сломать)
- ❌ Рандомные действия (только с разрешения)

---

**Хуманизация = плавные движения + рандомные задержки. Всё остальное - только с одобрения пользователя!** 🎯
