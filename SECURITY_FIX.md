# 🔐 Исправление безопасности - Токен в .env

## ✅ Что исправлено

### 1. Event Loop ошибка
**Было:** `There is no current event loop in thread 'TelegramBot'`
**Исправлено:** Создается новый event loop в потоке Telegram бота

### 2. Токен бота в коде
**Было:** Токен захардкожен в `telegram_notifier.py`
**Исправлено:** Токен загружается из `.env` файла

---

## 🚀 Как использовать

### 1. Установить зависимости

```bash
pip install python-telegram-bot python-dotenv
```

### 2. Создать .env файл

```bash
# Скопировать шаблон
cp .env.example .env
```

Отредактировать `.env`:

```env
TELEGRAM_BOT_TOKEN=8348805684:AAH8P4STd4-TP6nzGa-C5X7Z6_hjlFk9OAk
```

### 3. Запустить бота

```bash
python scripts/eve_farm_bot.py
```

✅ Telegram бот запустится автоматически!

---

## 🔒 Безопасность

### Файл .env
- ✅ **В .gitignore** - не попадет в git
- ✅ **Локальный** - только на твоей машине
- ✅ **Шаблон** - `.env.example` без токена для других

### Если токен утек в git

**1. Отозвать токен:**
- Зайти в @BotFather в Telegram
- Выбрать бота
- Revoke Token
- Получить новый токен

**2. Обновить .env:**
```env
TELEGRAM_BOT_TOKEN=новый_токен
```

**3. Очистить историю git (опционально):**
```bash
# Удалить файл из истории
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch eve/telegram_notifier.py" \
  --prune-empty --tag-name-filter cat -- --all

# Форс-пуш (ВНИМАНИЕ: перезапишет историю!)
git push origin --force --all
```

**ВАЖНО:** После форс-пуша все кто клонировал репо должны сделать:
```bash
git fetch origin
git reset --hard origin/main
```

---

## 📁 Структура файлов

```
eve-claude/
├── .env                    # ← ТОКЕН ЗДЕСЬ (НЕ в git)
├── .env.example            # ← Шаблон (БЕЗ токена)
├── .gitignore              # ← .env в списке
├── eve/
│   └── telegram_notifier.py  # ← Загружает из .env
└── scripts/
    ├── eve_farm_bot.py     # ← Запускает Telegram бота
    └── telegram_bot.py     # ← Устарел (проверяет .env)
```

---

## 🐛 Устранение проблем

### "TELEGRAM_BOT_TOKEN не найден"

```bash
# Проверь что .env файл существует
ls -la .env

# Проверь содержимое
cat .env

# Должно быть:
TELEGRAM_BOT_TOKEN=твой_токен_здесь
```

### "python-dotenv не установлен"

```bash
pip install python-dotenv
```

### "Event loop ошибка"

Обновлена функция `start_telegram_bot_background()` - создает новый loop.

---

## ✅ Чеклист

- [x] Токен убран из кода
- [x] Создан `.env.example` (без токена)
- [x] Создан `.env` (с токеном)
- [x] `.env` в `.gitignore`
- [x] Исправлена event loop ошибка
- [x] Обновлена документация

---

**Теперь безопасно! 🔐**
