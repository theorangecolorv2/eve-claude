# Anomaly Farmer Bot

Автоматический бот для фарма аномалий (убежища/укрытия) в EVE Online.

## Возможности

- ✅ Автоматический поиск аномалий в системе
- ✅ Варп к аномалиям (убежище/укрытие)
- ✅ Автоматическая зачистка всех целей
- ✅ Детекция экспедиций с уведомлениями в Telegram
- ✅ Автоматический прыжок в следующую систему по маршруту
- ✅ Статистика работы (аномалий/час, шанс экспедиции)
- ✅ Telegram бот для мониторинга

## Установка

```bash
# Установить зависимости
pip install -r requirements.txt

# Настроить Telegram (опционально)
# Создать .env файл в корне проекта:
TELEGRAM_BOT_TOKEN=your_bot_token_here
```

## Запуск

```bash
# Из корня проекта
python bots/anomaly_farmer/main.py

# Или старый путь (совместимость)
python scripts/eve_farm_bot.py
```

## Конфигурация

Файл `config.json`:

```json
{
  "bot_name": "Anomaly Farmer",
  "guns_key": "2",
  "support_modules_keys": ["4", "5", "6"],
  "max_systems": 0,
  "pause_between_systems": 3.0,
  "stats_log_interval": 5,
  "use_sanderling": false,
  "use_telegram": true
}
```

### Параметры

- **guns_key** - Клавиша активации пушек (по умолчанию "2")
- **support_modules_keys** - Клавиши модулей поддержки (активируются при варпе в первую аномалию)
- **max_systems** - Максимум систем для фарма (0 = бесконечно)
- **pause_between_systems** - Пауза между системами в секундах
- **stats_log_interval** - Логировать статистику каждые N систем
- **use_sanderling** - Использовать Sanderling для чтения памяти (в разработке)
- **use_telegram** - Отправлять уведомления в Telegram

## Подготовка

1. **Настроить корабль:**
   - Пушки на слот 2
   - Модули поддержки на слоты 4, 5, 6 (опционально)
   - Настроить Overview с вкладками "Jump" и "PvP Foe"

2. **Настроить маршрут:**
   - Создать маршрут через системы с аномалиями
   - Убедиться что маршрут активен

3. **Запустить бота:**
   - Окно EVE должно быть активно
   - Корабль должен быть в космосе (не в станции)

## Логика работы

1. Переключается на вкладку "Jump" в Overview
2. Ищет аномалии (убежище/укрытие)
3. Варпает к аномалии
4. Переключается на вкладку "PvP Foe"
5. Активирует модули поддержки (только в первой аномалии системы)
6. Ждёт появления целей
7. Лочит все цели (Ctrl+Click)
8. Убивает все цели (активирует пушки)
9. Проверяет popup экспедиции
10. Повторяет пока есть аномалии
11. Прыгает в следующую систему по маршруту

## Telegram уведомления

Бот отправляет уведомления:
- 🎉 При обнаружении экспедиции
- ❌ При критических ошибках (с скриншотом)

Команды Telegram бота:
- `/start` - Подписаться на уведомления и посмотреть статистику
- `/stats` - Текущая статистика работы
- `/users` - Количество подписчиков

## Статистика

Бот собирает статистику:
- Время работы
- Систем посещено
- Аномалий зачищено
- Экспедиций найдено
- Прыжков сделано
- Аномалий/час
- Шанс экспедиции (%)

Статистика сохраняется в `output/data/bot_stats.json`

## Логи

Логи сохраняются в `output/logs/farm_bot_YYYYMMDD_HHMMSS.log`

Каждое действие бота логируется для отладки и мониторинга.

## Безопасность

⚠️ **ВАЖНО**: Используйте только на тестовом сервере!

- Бот использует хуманизацию (плавные движения мыши, случайные задержки)
- Все действия логируются
- Telegram уведомления о критических ошибках

## Troubleshooting

**Бот не находит аномалии:**
- Убедитесь что вкладка "Jump" настроена в Overview
- Проверьте что шаблоны изображений актуальны (resources/assets/)

**Бот не переключается на PvP Foe:**
- Убедитесь что вкладка "PvP Foe" существует в Overview
- Проверьте шаблон `eve_tab_pvp_foe.png`

**Telegram уведомления не работают:**
- Проверьте что TELEGRAM_BOT_TOKEN указан в .env файле
- Убедитесь что библиотека установлена: `pip install python-telegram-bot`

**Бот не находит окно EVE:**
- Убедитесь что игра запущена
- Окно должно содержать "EVE" в заголовке
