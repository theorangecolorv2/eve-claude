# Sanderling - Модуль чтения памяти EVE Online

> **Статус**: 🚧 В разработке (Work In Progress)

---

## Что такое Sanderling?

**Sanderling** - это библиотека для чтения UI tree из памяти процесса EVE Online. Она позволяет получать данные о состоянии игры напрямую из памяти, без использования template matching.

### Преимущества перед template matching

| Template Matching | Sanderling (Memory Reading) |
|-------------------|----------------------------|
| ❌ Зависит от разрешения экрана | ✅ Не зависит от разрешения |
| ❌ Требует скриншоты UI элементов | ✅ Прямой доступ к данным |
| ❌ Медленнее (захват экрана + поиск) | ✅ Быстрее (прямое чтение) |
| ❌ Неточные данные (визуальные) | ✅ Точные данные (из памяти) |
| ❌ Не видит скрытые элементы | ✅ Видит всё UI tree |

### Что можно получить через Sanderling

- **Залоченные цели**: имя, тип, дистанция, HP (shield/armor/hull)
- **Overview**: список всех записей с координатами
- **Модули корабля**: состояние (активен/неактивен), количество патронов, перегрев
- **Состояние корабля**: HP, капаситор, скорость
- **UI элементы**: координаты любых элементов интерфейса

---

## Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                     EVE Online Process                       │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │                    UI Tree (Memory)                     │ │
│  │                                                          │ │
│  │  - Overview entries                                     │ │
│  │  - Locked targets                                       │ │
│  │  - Ship modules                                         │ │
│  │  - HUD elements                                         │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
                            ↓
                ┌───────────────────────┐
                │  read-memory-64-bit   │  ← C# executable
                │  (Sanderling)         │
                └───────────────────────┘
                            ↓
                    JSON UI Tree
                            ↓
        ┌───────────────────────────────────────┐
        │   eve/sanderling/ (Python)            │
        │                                       │
        │  ┌─────────────────────────────────┐ │
        │  │  service.py                     │ │  ← Фоновый сервис
        │  │  - Запуск read-memory-64-bit    │ │
        │  │  - Периодическое чтение         │ │
        │  │  - Кэширование данных           │ │
        │  └─────────────────────────────────┘ │
        │                                       │
        │  ┌─────────────────────────────────┐ │
        │  │  parser.py                      │ │  ← Парсер JSON
        │  │  - Извлечение целей             │ │
        │  │  - Извлечение Overview          │ │
        │  │  - Извлечение модулей           │ │
        │  └─────────────────────────────────┘ │
        │                                       │
        │  ┌─────────────────────────────────┐ │
        │  │  models.py                      │ │  ← Модели данных
        │  │  - Target                       │ │
        │  │  - OverviewEntry                │ │
        │  │  - Module                       │ │
        │  │  - ShipState                    │ │
        │  └─────────────────────────────────┘ │
        └───────────────────────────────────────┘
                            ↓
                    ┌───────────────┐
                    │   Bot Script  │
                    └───────────────┘
```

---

## Текущий статус

### ✅ Что работает

1. **Компиляция Sanderling** - `read-memory-64-bit.exe` скомпилирован и готов
2. **Базовая структура** - Python модули созданы (`service.py`, `parser.py`, `models.py`)
3. **Модели данных** - Определены классы для целей, Overview, модулей
4. **Парсер UI tree** - Базовая логика парсинга JSON

### 🚧 Что в процессе

1. **Интеграция с ботом** - Подключение к `eve_farm_bot.py`
2. **Тестирование парсера** - Проверка корректности извлечения данных
3. **Оптимизация** - Уменьшение времени чтения памяти
4. **Обработка ошибок** - Устойчивость к изменениям UI

### ❌ Что не работает / не реализовано

1. **HP целей** - Парсинг shield/armor/hull процентов
2. **Капаситор корабля** - Извлечение данных о капе
3. **Скорость корабля** - Парсинг текущей скорости
4. **Автоматический поиск UI root** - Долгая операция (~20 сек)
5. **Обработка обновлений игры** - Может сломаться после патчей

---

## Использование (когда будет готово)

### Пример 1: Базовое использование

```python
from eve.sanderling import SanderlingService

# Создать сервис
service = SanderlingService()

# Запустить (автоматически найдёт процесс EVE)
service.start()

# Получить данные
print(f"Залочено целей: {service.targets_count}")
print(f"В Overview: {service.overview_count}")

# Перебрать цели
for target in service.targets:
    print(f"  - {target.name} ({target.type}) at {target.distance}")
    if target.is_active:
        print(f"    ^ Активная цель")

# Остановить
service.stop()
```

### Пример 2: Интеграция с ботом

```python
from eve.sanderling import get_service, start_service

# Запустить глобальный сервис
start_service()

# Получить сервис
service = get_service()

# Использовать в боте
while True:
    # Проверить есть ли цели
    if service.targets_count > 0:
        print(f"Найдено {service.targets_count} целей")
        
        # Залочить первую цель (клик по координатам)
        first_target = service.targets[0]
        if first_target.center:
            x, y = first_target.center
            click(x, y)
    
    time.sleep(1)
```

### Пример 3: Проверка модулей

```python
# Получить модули корабля
modules = service.modules

# Найти активные пушки
active_guns = [m for m in modules if m.slot_type == 'high' and m.is_active]

print(f"Активных пушек: {len(active_guns)}")

# Проверить патроны
for gun in active_guns:
    if gun.ammo_count is not None:
        print(f"  {gun.slot_name}: {gun.ammo_count} патронов")
```

---

## Технические детали

### read-memory-64-bit.exe

**Расположение**: `sanderling-bin/read-memory-64-bit.exe`

**Использование**:
```bash
# Найти процесс EVE и UI root (медленно, ~20 сек)
read-memory-64-bit.exe read-memory-eve-online --pid=12345

# Читать с известным root address (быстро, ~100-300 мс)
read-memory-64-bit.exe read-memory-eve-online --pid=12345 --root-address=0x276A59E2CF8 --output-file=output.json
```

**Параметры**:
- `--pid` - ID процесса EVE Online
- `--root-address` - Адрес UI root (опционально, но сильно ускоряет)
- `--output-file` - Путь к выходному JSON файлу
- `--remove-other-dict-entries` - Убрать лишние поля (уменьшает размер JSON)

### Формат JSON

UI tree представлен как вложенная структура узлов:

```json
{
  "pythonObjectTypeName": "UIRoot",
  "dictEntriesOfInterest": {
    "_name": "root",
    "_displayX": 0,
    "_displayY": 0
  },
  "children": [
    {
      "pythonObjectTypeName": "TargetInBar",
      "dictEntriesOfInterest": {
        "_name": "target_0",
        "_displayX": 100,
        "_displayY": 200
      },
      "children": [...]
    }
  ]
}
```

### Типы узлов (pythonObjectTypeName)

- `TargetInBar` - Залоченная цель
- `OverviewScrollEntry` - Запись в Overview
- `ShipSlot` - Слот модуля корабля
- `EveLabelSmall` - Текстовый лейбл
- `ActiveTargetIndicator` - Индикатор активной цели
- `OverviewLabel` - Лейбл в Overview

---

## Roadmap

### Фаза 1: Базовая интеграция (текущая)

- [x] Компиляция read-memory-64-bit.exe
- [x] Создание Python модулей
- [x] Базовый парсер UI tree
- [ ] Тестирование на реальных данных
- [ ] Интеграция с eve_farm_bot.py

### Фаза 2: Улучшение парсера

- [ ] Парсинг HP целей (shield/armor/hull)
- [ ] Парсинг капаситора корабля
- [ ] Парсинг скорости корабля
- [ ] Обработка edge cases

### Фаза 3: Оптимизация

- [ ] Кэширование UI root address
- [ ] Уменьшение времени чтения (<100 мс)
- [ ] Фильтрация ненужных узлов
- [ ] Обработка ошибок и восстановление

### Фаза 4: Полная замена template matching

- [ ] Замена overview.py на Sanderling
- [ ] Замена hud.py на Sanderling
- [ ] Замена navigation.py на Sanderling
- [ ] Удаление assets/ (больше не нужны шаблоны)

---

## Проблемы и ограничения

### Известные проблемы

1. **Медленный поиск UI root** (~20 сек при первом запуске)
   - **Решение**: Кэшировать root address между запусками
   
2. **Большой размер JSON** (~5-10 MB)
   - **Решение**: Использовать `--remove-other-dict-entries`
   
3. **Изменения после патчей** - Структура UI tree может измениться
   - **Решение**: Fallback на template matching при ошибках

### Ограничения

- **Только Windows** - read-memory-64-bit.exe работает только на Windows
- **Только 64-bit клиент** - 32-bit клиент не поддерживается
- **Требует прав** - Чтение памяти требует прав администратора (иногда)

---

## Разработка

### Тестирование парсера

```bash
# Сделать снимок памяти
cd sanderling-bin
read-memory-64-bit.exe read-memory-eve-online --pid=12345 --output-file=test.json

# Протестировать парсер
cd ..
python -c "
import json
from eve.sanderling.parser import UITreeParser

with open('sanderling-bin/test.json') as f:
    data = json.load(f)

parser = UITreeParser(data)
state = parser.parse()

print(f'Targets: {len(state.targets)}')
print(f'Overview: {len(state.overview)}')
print(f'Modules: {len(state.ship.modules)}')
"
```

### Отладка

Включить debug логи:

```python
import logging
logging.basicConfig(level=logging.DEBUG)

from eve.sanderling import SanderlingService
service = SanderlingService()
service.start()
```

---

## Ссылки

- **Sanderling GitHub**: https://github.com/Arcitectus/Sanderling
- **Документация**: `Sanderling/guide/`
- **Исходники C#**: `Sanderling/implement/read-memory-64-bit/`

---

**Последнее обновление**: 2026-02-04

**Статус**: 🚧 В активной разработке
