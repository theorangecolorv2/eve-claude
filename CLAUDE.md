# CLAUDE.md - Инструкция для Claude Code агентов

> **ВАЖНО**: Прочитай этот документ ПОЛНОСТЬЮ перед началом работы над проектом.

---

## 🎮 О проекте

Это **исследовательский проект** по созданию бота для EVE Online в рамках улучшения анти-бот системы игры.

### Легальность и ограничения

**✅ РАЗРЕШЕНО:**
- Работа ТОЛЬКО на тестовом сервере (Singularity/Duality)
- Разрешение получено от CCP Alpha (сотрудник CCP Games)
- Публичный open-source проект для исследования
- Цель: помочь разработчикам улучшить защиту от ботов

**❌ СТРОГО ЗАПРЕЩЕНО:**
- Подключение к live серверу (Tranquility)
- Любые изменения настроек подключения к серверам
- Использование на реальных игровых аккаунтах
- Лаунчер уже настроен на тест-сервер - НЕ ТРОГАЙ настройки

---

## 📂 Структура проекта

```
eve-claude/
├── CLAUDE.md           # ← ТЫ ЗДЕСЬ (главная инструкция для агентов)
├── PROJECT.md          # Подробности проекта и легальность
├── HUMANIZATION.md     # Требования к человекоподобности
│
├── eve/                # Фреймворк автоматизации (v0.3.0)
│   │
│   │ # Базовые модули (для любой автоматизации Windows)
│   ├── mouse.py        # Мышь с хуманизацией (Bezier curves)
│   ├── keyboard.py     # Управление клавиатурой
│   ├── vision.py       # Поиск изображений (OpenCV template matching)
│   ├── screen.py       # Скриншоты
│   ├── window.py       # Управление окнами
│   ├── actions.py      # Высокоуровневые действия (click_on_image и т.д.)
│   │
│   │ # EVE-специфичные модули
│   ├── overview.py     # 🎯 Overview: подсчёт целей, лок, убийство
│   ├── hud.py          # HUD: проверка состояния пушек
│   ├── combat.py       # Управление дронами
│   ├── navigation.py   # Навигация: варп в аномалии, прыжки между системами
│   ├── telegram_notifier.py  # Telegram уведомления
│   │
│   └── sanderling/     # 🚧 Модуль чтения памяти (в разработке)
│       ├── service.py  # Фоновый сервис чтения памяти
│       ├── parser.py   # Парсер UI tree
│       └── models.py   # Модели данных (Target, OverviewEntry, Module)
│
├── inbox/              # 📥 СЮДА пользователь кладет скриншоты UI элементов
├── assets/             # 📦 СЮДА ты перемещаешь обработанные изображения
├── scripts/            # 🤖 СЮДА ты пишешь скрипты автоматизации
│   ├── eve_farm_bot.py      # Основной бот для фарма аномалий
│   ├── telegram_bot.py      # Telegram бот для управления
│   └── test_*.py            # Тестовые скрипты
├── data/               # Данные бота (статистика, пользователи Telegram)
├── Sanderling/         # Исходники Sanderling (C#)
├── sanderling-bin/     # Скомпилированный read-memory-64-bit.exe
└── dev_tools/          # 🛠 Утилиты для разработки
```

---

## 🔄 Рабочий процесс (ОСНОВНОЙ WORKFLOW)

### 1. Пользователь описывает задачу

Пример:
```
Пользователь: "Нужно автоматизировать процесс майнинга в EVE:
1. Открыть Probe Scanner
2. Кликнуть на аномалию
3. Нажать Warp To"
```

### 2. Пользователь делает скриншоты UI элементов

Пользователь использует `Win+Shift+S` и сохраняет скриншоты в `inbox/`:
- `Screenshot_1.png` - кнопка Probe Scanner
- `Screenshot_2.png` - строка аномалии
- `Screenshot_3.png` - кнопка Warp To

**ВАЖНО**: Пользователь вырезает элементы САМ. НЕ пытайся вырезать через crop.py - это неэффективно.

### 3. Ты проверяешь inbox/

```bash
ls inbox/
# или
dir inbox
```

### 4. Ты смотришь изображения

```python
Read("inbox/Screenshot_1.png")
Read("inbox/Screenshot_2.png")
Read("inbox/Screenshot_3.png")
```

### 5. Спроси пользователя что на скриншотах (если неясно)

```
Ты: "Вижу 3 файла в inbox/:
- Screenshot_1.png - похоже на иконку сканера
- Screenshot_2.png - строка в списке
- Screenshot_3.png - кнопка

Подтверди: это Probe Scanner кнопка, строка аномалии, и Warp To?"
```

### 6. Переименуй и перемести в assets/

Используй понятные имена по паттерну: `eve_{module}_{element}.png`

```bash
cp inbox/Screenshot_1.png assets/eve_probe_scanner_button.png
cp inbox/Screenshot_2.png assets/eve_anomaly_row.png
cp inbox/Screenshot_3.png assets/eve_warp_to_button.png
```

### 6.1. ОЧИСТКА inbox/ (ОБЯЗАТЕЛЬНО!)

**После копирования - УДАЛЯЙ файлы из inbox/**

```bash
rm inbox/Screenshot_1.png
rm inbox/Screenshot_2.png
rm inbox/Screenshot_3.png

# Или всё сразу
rm inbox/*.png
```

**Почему критично:**
- Экономия токенов (не читать сотни старых файлов)
- Inbox остается чистым для новых задач

### 7. Создай скрипт в scripts/

Используй шаблон из `templates/` (если есть) или создай новый.

**Обязательные требования к скриптам:**
- ✅ Подробное логирование (logger.info/debug)
- ✅ Обработка ошибок (try/except с логами)
- ✅ Таймауты для всех wait_image()
- ✅ Комментарии на русском
- ✅ Использование функций хуманизации (см. HUMANIZATION.md)

---

## 🖱 Хуманизация (ВСТРОЕНА в eve/)

**Хуманизация теперь ВСТРОЕНА в модуль eve/ - не нужно писать вручную!**

### Просто используй функции из eve/

```python
from eve import click_on_image, double_click_on_image, right_click_menu

# Всё уже хуманизировано: Bezier curves, random delays, смещение от центра
click_on_image("button.png")
double_click_on_image("anomaly.png")
right_click_menu("target.png", "orbit.png")
```

### Если нужно кликнуть по координатам

```python
from eve import click

click(500, 300)  # УЖЕ хуманизировано (Bezier + delays)
click(500, 300, humanize=False)  # Быстрый клик для отладки
```

### Между действиями используй random_delay

```python
from eve import click_on_image, random_delay

click_on_image("button1.png")
random_delay(0.3, 0.8)  # Пауза между действиями
click_on_image("button2.png")
```

### ⚠️ Что НЕ делать

- ❌ Случайные ошибки/промахи (может сломать бота)
- ❌ Рандомные действия без разрешения пользователя
- ❌ Прямые вызовы pyautogui (используй eve/)

### Настройка хуманизации (если нужно)

```python
from eve import HumanConfig

HumanConfig.MOVE_DURATION_MAX = 0.5      # Скорость движения
HumanConfig.CLICK_OFFSET_MAX = 5          # Смещение от центра (px)
HumanConfig.BEZIER_CONTROL_POINTS = 2     # Контрольные точки кривой
```

---

## 🎮 EVE-специфичные функции

### Overview (подсчёт и лок целей)

```python
from eve import (
    is_overview_empty,      # Проверка пустого Overview
    has_locked_targets,     # Есть ли залоченные цели
    count_targets,          # Подсчёт целей в Overview
    lock_all_targets,       # Залочить все цели (Ctrl+Click)
    kill_locked_targets,    # Убить залоченные цели
    lock_and_kill,          # Залочить и убить пачку
    clear_anomaly,          # Полная зачистка аномалии
)

# Пример: зачистка аномалии
killed = clear_anomaly(guns_key="2")
logger.info(f"Убито целей: {killed}")
```

### HUD (проверка пушек)

```python
from eve import are_guns_active, wait_guns_deactivate

# Проверить активны ли пушки
if are_guns_active():
    logger.info("Пушки стреляют")

# Ждать пока пушки не выключатся
wait_guns_deactivate(timeout=60)
```

### Combat (дроны)

```python
from eve import launch_drones, engage_drones, recall_drones

launch_drones("shift+f")    # Запустить дронов
engage_drones("f")          # Дроны атакуют
recall_drones("shift+r")    # Вернуть дронов
```

---

## 📝 Требования к коду

### 1. Логирование

**ОБЯЗАТЕЛЬНО** используй logging во всех скриптах:

```python
import logging

logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s [%(levelname)s] %(message)s',
    handlers=[
        logging.FileHandler('logs/bot.log'),
        logging.StreamHandler()
    ]
)
logger = logging.getLogger(__name__)

# В коде:
logger.info("Активирую окно EVE Online...")
logger.debug(f"Ищу изображение: {template}")
logger.error(f"Не найден элемент UI: {template}")
logger.warning("Таймаут при ожидании элемента")
```

### 2. Обработка ошибок

```python
try:
    if not click_on_image("eve_button.png", timeout=10):
        logger.error("Кнопка не найдена")
        return False
except Exception as e:
    logger.exception(f"Ошибка при клике: {e}")
    return False
```

### 3. Структура скрипта

```python
"""
Описание: Автоматизация [что делает скрипт]
Файл: scripts/eve_[название].py
"""

import sys
import os
import logging

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from eve import (
    activate_window,
    # Действия с изображениями (хуманизация встроена)
    click_on_image,
    double_click_on_image,
    right_click_on_image,
    right_click_menu,
    # Ожидание
    wait_and_click,
    is_visible,
    wait_until_gone,
    # Утилиты
    random_delay,
)

# Логирование
logger = logging.getLogger(__name__)

def main():
    """Главная функция скрипта."""
    logger.info("Запуск автоматизации...")

    # Активация окна EVE
    if not activate_window("EVE"):
        logger.error("Окно EVE не найдено")
        return False

    # Пример: клик по кнопке (хуманизация автоматическая)
    if not click_on_image("eve_button.png", timeout=10):
        logger.error("Кнопка не найдена")
        return False

    random_delay(0.3, 0.7)  # Пауза между действиями

    logger.info("Автоматизация завершена успешно")
    return True

if __name__ == "__main__":
    logging.basicConfig(
        level=logging.INFO,
        format='%(asctime)s [%(levelname)s] %(message)s'
    )
    main()
```

---

## 🎯 Примеры работы

### Пример 1: Простая автоматизация

**Задача**: Открыть меню и кликнуть кнопку

**Процесс**:
1. Пользователь положил в inbox/: `menu_button.png`, `option.png`
2. Ты спросил: "Это кнопка открытия меню и опция внутри?"
3. Переименовал: `eve_menu_button.png`, `eve_option.png`
4. Написал скрипт с логированием и хуманизацией
5. Протестировал

### Пример 2: Сложная цепочка действий

См. `scripts/notepad_save_as.py` как пример структуры.

---

## ⚠️ Частые ошибки и как их избежать

### ❌ Ошибка 1: Попытка вырезать элементы через crop.py

**НЕ ДЕЛАЙ ТАК:**
```bash
python dev_tools/crop.py crop screenshot.png 100 200 300 400
```

Это неэффективно (много попыток, много токенов).

**ПРАВИЛЬНО:**
Пользователь сам вырезает через Win+Shift+S и кладет в inbox/.

### ❌ Ошибка 2: Прямое использование pyautogui

**НЕ ДЕЛАЙ ТАК:**
```python
import pyautogui
pyautogui.click(x, y)  # Телепортация - детектится
```

**ПРАВИЛЬНО:**
```python
from eve import click
click(x, y)  # Хуманизация встроена (Bezier + delays)
```

### ❌ Ошибка 3: Нет логирования

**НЕ ДЕЛАЙ ТАК:**
```python
click_on_image("button.png")  # Тишина, если сломается
```

**ПРАВИЛЬНО:**
```python
logger.info("Кликаю на кнопку...")
if not click_on_image("button.png", timeout=10):
    logger.error("Кнопка не найдена")
    return False
logger.info("Клик выполнен успешно")
```

---

## 📝 ПРАВИЛА ДОКУМЕНТАЦИИ

**КРИТИЧЕСКИ ВАЖНО - ЧИТАЙ ВНИМАТЕЛЬНО:**

### ❌ ЧТО НЕ ДЕЛАТЬ

**НЕ создавай кучу MD файлов на каждую мелкую функцию:**
- ❌ CHANGELOG_[feature].md для каждой фичи
- ❌ QUICK_START_[feature].md для каждой фичи  
- ❌ [FEATURE]_GUIDE.md для каждой фичи
- ❌ Десятки отдельных документов

**Это тратит время и засоряет проект!**

### ✅ ЧТО ДЕЛАТЬ

**Поддерживай ТОЛЬКО основную документацию:**

1. **README.md** - главный файл проекта
   - Обзор проекта
   - Структура
   - Быстрый старт
   - Основные возможности

2. **docs/[MODULE].md** - ОДНА документация на модуль
   - Например: `docs/INVENTORY.md` для всего модуля инвентаря
   - Обновляй существующий файл, НЕ создавай новые

3. **CLAUDE.md** - инструкции для агентов (этот файл)
   - Обновляй при изменении workflow

### Когда обновлять документацию

**Обновляй существующие файлы:**
```python
# Добавил новый метод в inventory.py?
# → Обнови docs/INVENTORY.md (добавь метод в API Reference)

# Изменил workflow?
# → Обнови CLAUDE.md

# Добавил новую возможность?
# → Обнови README.md (раздел "Возможности")
```

**НЕ создавай новые файлы** для каждого изменения!

### Примеры

**❌ ПЛОХО:**
```
docs/
├── INVENTORY.md
├── INVENTORY_QUICK_START.md      ← НЕ НУЖНО
├── INVENTORY_API.md               ← НЕ НУЖНО
├── INVENTORY_EXAMPLES.md          ← НЕ НУЖНО
├── CHANGELOG_INVENTORY.md         ← НЕ НУЖНО
├── CHANGELOG_INVENTORY_v2.md      ← НЕ НУЖНО
└── CONFIG_GUIDE.md                ← НЕ НУЖНО (если уже есть в README)
```

**✅ ХОРОШО:**
```
docs/
├── INVENTORY.md          ← ВСЯ информация об инвентаре здесь
├── SANDERLING.md         ← ВСЯ информация о Sanderling здесь
└── QUICK_START.md        ← Общий quick start для ВСЕГО проекта
```

### Changelog

**НЕ создавай отдельные CHANGELOG файлы!**

Если нужно отследить изменения:
- Используй git commits
- Или добавь раздел "История изменений" в конец основного MD файла

**Пример:**
```markdown
# docs/INVENTORY.md

## API Reference
...

## История изменений

### v0.5.1 (2026-02-04)
- Добавлен автоматическое открытие инвентаря
- Улучшена активация фильтров

### v0.5.0 (2026-02-03)
- Первая версия модуля
```

### Итого

**Золотое правило:**
> Один модуль = один MD файл в docs/
> 
> Обновляй существующие, НЕ создавай новые!

---

## 📚 Дополнительные документы

- **PROJECT.md** - Подробности проекта и легальность
- **WORKFLOW.md** - Детальный процесс работы с inbox/
- **HUMANIZATION.md** - Требования к человекоподобности бота
- **COMMANDS.md** - Справка по dev_tools утилитам

---

## 🚀 Быстрый старт

Когда пользователь просит автоматизировать что-то:

1. ✅ Прочитай задачу
2. ✅ Проверь `ls inbox/`
3. ✅ Посмотри изображения через Read
4. ✅ Спроси уточнения если нужно
5. ✅ Переименуй и перемести в assets/
6. ✅ **ОЧИСТЬ inbox/** (rm inbox/*.png)
7. ✅ Создай скрипт с логированием и хуманизацией
8. ✅ Протестируй (или попроси пользователя)

**Помни**: Качество кода и логирование важнее скорости. Бот должен быть отлаживаемым и человекоподобным.

---

## 🔐 Безопасность

- ❌ Никогда не изменяй настройки подключения к серверам
- ❌ Никогда не пытайся подключиться к Tranquility
- ✅ Всегда работай только с тестовым сервером
- ✅ Весь код публичный - пиши так, будто его читают разработчики CCP

---

**Удачи в разработке! 🚀**


---

## 🔬 Sanderling - Чтение памяти (в разработке)

**Статус**: 🚧 Work In Progress - НЕ используй в продакшн коде пока не готово!

### Что это?

**Sanderling** - модуль для чтения UI tree из памяти EVE Online. Позволяет получать данные напрямую из памяти, без template matching.

**Преимущества**:
- ✅ Не зависит от разрешения экрана
- ✅ Точные данные (HP целей, состояние модулей)
- ✅ Быстрее template matching
- ✅ Видит скрытые UI элементы

**Недостатки**:
- ❌ Сложнее в настройке
- ❌ Может сломаться после патчей игры
- ❌ Требует компиляции C# кода

### Текущий статус

- [x] Компиляция read-memory-64-bit.exe
- [x] Базовая структура Python модулей
- [ ] Тестирование на реальных данных
- [ ] Интеграция с eve_farm_bot.py

### Когда использовать

**ПОКА НЕ ИСПОЛЬЗУЙ** в основном боте. Sanderling ещё не готов к продакшн использованию.

Когда будет готово, можно будет заменить:
- `overview.py` → Sanderling (подсчёт целей, лок)
- `hud.py` → Sanderling (проверка пушек)
- `navigation.py` → Sanderling (поиск аномалий)

### Документация

См. **SANDERLING.md** для детальной информации:
- Архитектура
- Использование
- Roadmap
- Технические детали

---

**Помни**: Основной бот работает на template matching и это НОРМАЛЬНО. Sanderling - это будущее улучшение, не блокер.

