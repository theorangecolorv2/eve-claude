# CLAUDE.md - Инструкция для Claude Code агентов

> **ВАЖНО**: Прочитай этот документ ПОЛНОСТЬЮ перед началом работы над проектом.

---

## 🎮 О проекте

Это **исследовательский проект** по созданию бота для EVE Online в рамках улучшения анти-бот системы игры.

### Легальность и ограничения

**✅ РАЗРЕШЕНО:**
- Работа ТОЛЬКО на тестовом сервере (Singularity/Duality)
- Разрешение получено от CCP Alpha (сотрудник CCP Games)
- Публичный open-source проект для исследования
- Цель: помочь разработчикам улучшить защиту от ботов

**❌ СТРОГО ЗАПРЕЩЕНО:**
- Подключение к live серверу (Tranquility)
- Любые изменения настроек подключения к серверам
- Использование на реальных игровых аккаунтах
- Лаунчер уже настроен на тест-сервер - НЕ ТРОГАЙ настройки

---

## 📂 Структура проекта

```
eve-claude/
├── CLAUDE.md           # ← ТЫ ЗДЕСЬ (главная инструкция для агентов)
├── PROJECT.md          # Подробности проекта и легальность
├── WORKFLOW.md         # Детальный процесс работы
├── HUMANIZATION.md     # Требования к человекоподобности
│
├── eve/                # Фреймворк автоматизации
│   ├── screen.py       # Скриншоты
│   ├── vision.py       # Поиск изображений (OpenCV template matching)
│   ├── mouse.py        # Управление мышью
│   ├── keyboard.py     # Управление клавиатурой
│   └── window.py       # Управление окнами
│
├── inbox/              # 📥 СЮДА пользователь кладет скриншоты UI элементов
├── assets/             # 📦 СЮДА ты перемещаешь обработанные изображения
├── scripts/            # 🤖 СЮДА ты пишешь скрипты автоматизации
├── templates/          # 📋 Шаблоны для скриптов
├── dev_tools/          # 🛠 Утилиты для разработки
│   ├── capture.py      # Создание скриншотов
│   ├── crop.py         # Обрезка изображений (НЕ ИСПОЛЬЗУЙ - пользователь режет сам)
│   └── windows.py      # Управление окнами
└── temp/               # Временные файлы
```

---

## 🔄 Рабочий процесс (ОСНОВНОЙ WORKFLOW)

### 1. Пользователь описывает задачу

Пример:
```
Пользователь: "Нужно автоматизировать процесс майнинга в EVE:
1. Открыть Probe Scanner
2. Кликнуть на аномалию
3. Нажать Warp To"
```

### 2. Пользователь делает скриншоты UI элементов

Пользователь использует `Win+Shift+S` и сохраняет скриншоты в `inbox/`:
- `Screenshot_1.png` - кнопка Probe Scanner
- `Screenshot_2.png` - строка аномалии
- `Screenshot_3.png` - кнопка Warp To

**ВАЖНО**: Пользователь вырезает элементы САМ. НЕ пытайся вырезать через crop.py - это неэффективно.

### 3. Ты проверяешь inbox/

```bash
ls inbox/
# или
dir inbox
```

### 4. Ты смотришь изображения

```python
Read("inbox/Screenshot_1.png")
Read("inbox/Screenshot_2.png")
Read("inbox/Screenshot_3.png")
```

### 5. Спроси пользователя что на скриншотах (если неясно)

```
Ты: "Вижу 3 файла в inbox/:
- Screenshot_1.png - похоже на иконку сканера
- Screenshot_2.png - строка в списке
- Screenshot_3.png - кнопка

Подтверди: это Probe Scanner кнопка, строка аномалии, и Warp To?"
```

### 6. Переименуй и перемести в assets/

Используй понятные имена по паттерну: `eve_{module}_{element}.png`

```bash
cp inbox/Screenshot_1.png assets/eve_probe_scanner_button.png
cp inbox/Screenshot_2.png assets/eve_anomaly_row.png
cp inbox/Screenshot_3.png assets/eve_warp_to_button.png
```

### 6.1. ОЧИСТКА inbox/ (ОБЯЗАТЕЛЬНО!)

**После копирования - УДАЛЯЙ файлы из inbox/**

```bash
rm inbox/Screenshot_1.png
rm inbox/Screenshot_2.png
rm inbox/Screenshot_3.png

# Или всё сразу
rm inbox/*.png
```

**Почему критично:**
- Экономия токенов (не читать сотни старых файлов)
- Inbox остается чистым для новых задач

### 7. Создай скрипт в scripts/

Используй шаблон из `templates/` (если есть) или создай новый.

**Обязательные требования к скриптам:**
- ✅ Подробное логирование (logger.info/debug)
- ✅ Обработка ошибок (try/except с логами)
- ✅ Таймауты для всех wait_image()
- ✅ Комментарии на русском
- ✅ Использование функций хуманизации (см. HUMANIZATION.md)

---

## 🖱 Требования к хуманизации

**КРИТИЧЕСКИ ВАЖНО**: Бот должен вести себя максимально человекоподобно.

### Движения мыши

❌ **НЕ используй:**
```python
pyautogui.click(x, y)  # Телепортация курсора - детектится ботодетектором
```

✅ **ВСЕГДА используй:**
```python
from eve.mouse import click, move_to

move_to(x, y, duration=random.uniform(0.3, 0.7))  # Плавное движение
time.sleep(random.uniform(0.1, 0.3))              # Пауза перед кликом
click(x, y)
```

### Таймеры и задержки

❌ **Фиксированные задержки:**
```python
time.sleep(1.0)  # Детектится как бот
```

✅ **Рандомизированные задержки:**
```python
import random
time.sleep(random.uniform(0.8, 1.5))  # Естественная вариативность
```

### ⚠️ Что НЕ делать

**НЕ добавляй:**
- ❌ Случайные ошибки/промахи курсором (может сломать бота)
- ❌ Рандомные действия (проверка чата, движение камеры) без явного разрешения пользователя

**Хуманизация = плавные движения + рандомные задержки.** Всё остальное только с одобрения!

### Подробнее читай в HUMANIZATION.md

---

## 📝 Требования к коду

### 1. Логирование

**ОБЯЗАТЕЛЬНО** используй logging во всех скриптах:

```python
import logging

logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s [%(levelname)s] %(message)s',
    handlers=[
        logging.FileHandler('logs/bot.log'),
        logging.StreamHandler()
    ]
)
logger = logging.getLogger(__name__)

# В коде:
logger.info("Активирую окно EVE Online...")
logger.debug(f"Ищу изображение: {template}")
logger.error(f"Не найден элемент UI: {template}")
logger.warning("Таймаут при ожидании элемента")
```

### 2. Обработка ошибок

```python
try:
    if not click_on_image("eve_button.png", timeout=10):
        logger.error("Кнопка не найдена")
        return False
except Exception as e:
    logger.exception(f"Ошибка при клике: {e}")
    return False
```

### 3. Структура скрипта

```python
"""
Описание: Автоматизация [что делает скрипт]
Файл: scripts/eve_[название].py
"""

import sys
import os
import time
import random
import logging

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from eve import activate_window, click_on_image, wait_image, type_text
from eve.mouse import move_to

# Логирование
logger = logging.getLogger(__name__)

def main_function():
    """Главная функция скрипта."""
    logger.info("Запуск автоматизации...")

    # Активация окна EVE
    if not activate_window("EVE"):
        logger.error("Окно EVE не найдено")
        return False

    # ... остальная логика

    logger.info("Автоматизация завершена успешно")
    return True

if __name__ == "__main__":
    logging.basicConfig(level=logging.INFO)
    main_function()
```

---

## 🎯 Примеры работы

### Пример 1: Простая автоматизация

**Задача**: Открыть меню и кликнуть кнопку

**Процесс**:
1. Пользователь положил в inbox/: `menu_button.png`, `option.png`
2. Ты спросил: "Это кнопка открытия меню и опция внутри?"
3. Переименовал: `eve_menu_button.png`, `eve_option.png`
4. Написал скрипт с логированием и хуманизацией
5. Протестировал

### Пример 2: Сложная цепочка действий

См. `scripts/notepad_save_as.py` как пример структуры.

---

## ⚠️ Частые ошибки и как их избежать

### ❌ Ошибка 1: Попытка вырезать элементы через crop.py

**НЕ ДЕЛАЙ ТАК:**
```bash
python dev_tools/crop.py crop screenshot.png 100 200 300 400
```

Это неэффективно (много попыток, много токенов).

**ПРАВИЛЬНО:**
Пользователь сам вырезает через Win+Shift+S и кладет в inbox/.

### ❌ Ошибка 2: Телепортация курсора

**НЕ ДЕЛАЙ ТАК:**
```python
click(x, y)  # Мгновенный прыжок
```

**ПРАВИЛЬНО:**
```python
move_to(x, y, duration=0.5)  # Плавное движение
time.sleep(0.2)
click(x, y)
```

### ❌ Ошибка 3: Нет логирования

**НЕ ДЕЛАЙ ТАК:**
```python
click_on_image("button.png")  # Тишина, если сломается
```

**ПРАВИЛЬНО:**
```python
logger.info("Кликаю на кнопку...")
if not click_on_image("button.png", timeout=10):
    logger.error("Кнопка не найдена")
    return False
logger.info("Клик выполнен успешно")
```

---

## 📚 Дополнительные документы

- **PROJECT.md** - Подробности проекта и легальность
- **WORKFLOW.md** - Детальный процесс работы с inbox/
- **HUMANIZATION.md** - Требования к человекоподобности бота
- **COMMANDS.md** - Справка по dev_tools утилитам

---

## 🚀 Быстрый старт

Когда пользователь просит автоматизировать что-то:

1. ✅ Прочитай задачу
2. ✅ Проверь `ls inbox/`
3. ✅ Посмотри изображения через Read
4. ✅ Спроси уточнения если нужно
5. ✅ Переименуй и перемести в assets/
6. ✅ **ОЧИСТЬ inbox/** (rm inbox/*.png)
7. ✅ Создай скрипт с логированием и хуманизацией
8. ✅ Протестируй (или попроси пользователя)

**Помни**: Качество кода и логирование важнее скорости. Бот должен быть отлаживаемым и человекоподобным.

---

## 🔐 Безопасность

- ❌ Никогда не изменяй настройки подключения к серверам
- ❌ Никогда не пытайся подключиться к Tranquility
- ✅ Всегда работай только с тестовым сервером
- ✅ Весь код публичный - пиши так, будто его читают разработчики CCP

---

**Удачи в разработке! 🚀**
