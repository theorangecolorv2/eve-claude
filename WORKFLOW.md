# WORKFLOW.md - Процесс работы с автоматизацией

> Подробная инструкция по процессу создания скриптов автоматизации для EVE Online.

---

## 📋 Общий процесс

```
1. Пользователь описывает задачу
   ↓
2. Пользователь делает скриншоты UI → inbox/
   ↓
3. Claude проверяет inbox/ и смотрит изображения
   ↓
4. Claude уточняет детали (если нужно)
   ↓
5. Claude переименовывает и перемещает → assets/
   ↓
6. Claude ОЧИЩАЕТ inbox/
   ↓
7. Claude пишет скрипт → scripts/
   ↓
8. Тестирование и итерация
```

---

## Шаг 1: Получение задачи от пользователя

### Пример ответа пользователю

```
Понял задачу. Для автоматизации нужны скриншоты:
1. Кнопка/иконка Probe Scanner
2. Строка аномалии в списке
3. Кнопка Warp To

Сделай скриншоты через Win+Shift+S и сохрани в inbox/.
Дай знать когда готово.
```

---

## Шаг 2-4: Проверка и уточнение

```bash
ls inbox/
```

```python
Read("inbox/Screenshot_1.png")
Read("inbox/Screenshot_2.png")
```

---

## Шаг 5-6: Переименование и ОЧИСТКА

### Правила именования

Формат: `eve_{module}_{element}.png`

```
eve_overview_header_name.png    # Заголовок Overview
eve_hud_anchor.png              # Anchor для HUD
eve_lock_anchor.png             # Anchor залоченной цели
eve_tab_pvp_foe.png             # Вкладка PvP Foe
```

### Команды

```bash
# Копируем в assets/
cp inbox/Screenshot_1.png assets/eve_button.png

# ОБЯЗАТЕЛЬНО чистим inbox/
rm inbox/Screenshot_1.png
```

---

## Шаг 7: Создание скрипта

### Шаблон скрипта (актуальный)

```python
"""
Автоматизация: [Краткое описание]
"""

import sys
import os
import logging

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from eve import (
    activate_window,
    random_delay,
    # Базовые действия
    click_on_image,
    is_visible,
    # EVE-специфичные
    count_targets,
    lock_all_targets,
    clear_anomaly,
    are_guns_active,
)

logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s [%(levelname)s] %(name)s: %(message)s'
)
logger = logging.getLogger(__name__)


def main():
    logger.info("Запуск автоматизации...")

    if not activate_window("EVE"):
        logger.error("Окно EVE не найдено")
        return False

    # Пример: зачистка аномалии
    killed = clear_anomaly(guns_key="2")
    logger.info(f"Убито целей: {killed}")

    return True


if __name__ == "__main__":
    main()
```

### Ключевые требования

✅ **ОБЯЗАТЕЛЬНО:**
- Подробное логирование
- Использование `from eve import ...` (хуманизация встроена)
- `random_delay()` между действиями
- Обработка ошибок

❌ **ЗАПРЕЩЕНО:**
- Прямые вызовы pyautogui
- Хардкод координат (кроме overview где есть детект)
- Молчаливые ошибки

---

## Типичные проблемы

**Проблема**: Элемент не найден
**Решение**: Проверь имя файла, убедись что элемент видим, уменьши confidence

**Проблема**: Бот слишком быстрый
**Решение**: Добавь `random_delay()` между действиями

---

## Checklist перед завершением

- [ ] Все скриншоты из inbox/ обработаны
- [ ] inbox/ ОЧИЩЕН
- [ ] Файлы в assets/ с правильными именами
- [ ] Скрипт содержит логирование
- [ ] Используется `from eve import ...`

---

**Основная документация в CLAUDE.md!**
