# WORKFLOW.md - Процесс работы с автоматизацией

> Подробная инструкция по процессу создания скриптов автоматизации для EVE Online.

---

## 📋 Общий процесс

```
1. Пользователь описывает задачу
   ↓
2. Пользователь делает скриншоты UI → inbox/
   ↓
3. Claude проверяет inbox/ и смотрит изображения
   ↓
4. Claude уточняет детали (если нужно)
   ↓
5. Claude переименовывает и перемещает → assets/
   ↓
6. Claude пишет скрипт → scripts/
   ↓
7. Тестирование и итерация
```

---

## Шаг 1: Получение задачи от пользователя

### Пример задачи

```
Пользователь: "Нужно автоматизировать процесс майнинга:
1. Открыть Probe Scanner
2. Найти аномалию типа 'Small Asteroid Cluster'
3. Кликнуть Warp To
4. Подлететь к астероиду
5. Включить майнеры
6. Ждать заполнения карго"
```

### Что делать

1. **Прочитай задачу внимательно**
2. **Определи список UI элементов**, которые понадобятся:
   - Кнопка Probe Scanner
   - Строка с названием аномалии
   - Кнопка Warp To
   - Иконка астероида
   - Кнопки Mining Laser
   - Индикатор карго
3. **Сообщи пользователю** список элементов, которые нужно заскриншотить

**Пример ответа:**
```
Понял задачу. Для автоматизации нужны скриншоты:
1. Кнопка/иконка Probe Scanner
2. Строка аномалии "Small Asteroid Cluster" в списке
3. Кнопка Warp To
4. Иконка астероида в space
5. Кнопки Mining Laser (активные и неактивные состояния)
6. Индикатор заполнения карго

Сделай скриншоты через Win+Shift+S и сохрани в inbox/.
Дай знать когда готово.
```

---

## Шаг 2: Пользователь делает скриншоты

### Инструкция для пользователя

1. Нажми `Win + Shift + S`
2. Выдели нужный элемент UI мышью
3. Скриншот скопирован в буфер обмена
4. Открой Paint или любой редактор
5. Вставь (`Ctrl+V`) и сохрани в `C:\opt\eve-claude\inbox\`
6. Имя файла не важно (будет переименовано)

### Альтернативный способ

- Snipping Tool (Windows встроенный)
- ShareX
- Greenshot
- Любой инструмент скриншотов

---

## Шаг 3: Claude проверяет inbox/

### Команды для проверки

```bash
# Windows
dir inbox

# Linux/Mac
ls inbox/
```

### Просмотр изображений

```python
Read("inbox/Screenshot_1.png")
Read("inbox/Screenshot_2.png")
Read("inbox/Screenshot_3.png")
# ... и так далее
```

**Смотри ВСЕ файлы** в inbox/.

---

## Шаг 4: Уточнение деталей

### Когда нужно уточнять

- Непонятно что изображено
- Несколько похожих элементов
- Нужно знать контекст использования
- Разные состояния элемента (active/inactive)

### Как спрашивать

**Плохо:**
```
"Что это?"
```

**Хорошо:**
```
"Вижу 5 файлов в inbox/:

1. Screenshot_1.png - похоже на иконку сканера в левом верхнем углу
2. Screenshot_2.png - строка с текстом в списке (аномалия?)
3. Screenshot_3.png - синяя кнопка с текстом 'Warp To'?
4. Screenshot_4.png - кружок/сфера (астероид?)
5. Screenshot_5.png - две иконки лазеров (разные состояния?)

Подтверди:
- #1 - это Probe Scanner кнопка?
- #2 - строка аномалии в списке?
- #3 - кнопка Warp To?
- #4 - иконка астероида для таргетинга?
- #5 - Mining Laser активный и неактивный?
```

### Важно

- Описывай что видишь на изображении
- Задавай конкретные вопросы
- Не придумывай - спрашивай если непонятно

---

## Шаг 5: Переименование и перемещение в assets/

### Правила именования

Формат: `eve_{module}_{element}_{state}.png`

**Примеры:**
```
eve_probe_scanner_button.png          # Кнопка Probe Scanner
eve_anomaly_small_cluster.png         # Строка аномалии
eve_warp_to_button.png                # Кнопка Warp To
eve_asteroid_veldspar_icon.png        # Иконка астероида
eve_mining_laser_active.png           # Лазер активный
eve_mining_laser_inactive.png         # Лазер неактивный
eve_cargo_full_indicator.png          # Индикатор полного карго
```

### Структура имени

```
eve_{модуль/контекст}_{элемент}_{состояние}.png
    └─ игра        └─ что это  └─ опционально (active/inactive/full/empty)
```

### Команды

```bash
# Копирование в assets/
cp inbox/Screenshot_1.png assets/eve_probe_scanner_button.png
cp inbox/Screenshot_2.png assets/eve_anomaly_small_cluster.png
cp inbox/Screenshot_3.png assets/eve_warp_to_button.png
# ... и так далее
```

### Очистка inbox/ (ОБЯЗАТЕЛЬНО!)

**После копирования в assets/ - УДАЛЯЙ файлы из inbox/**

```bash
# Удаление обработанных файлов
rm inbox/Screenshot_1.png
rm inbox/Screenshot_2.png
rm inbox/Screenshot_3.png
# ... и так далее

# Или всё сразу (если все файлы обработаны)
rm inbox/*.png
```

**Почему обязательно чистить:**
- Экономия токенов (не читать сотни старых скриншотов)
- Inbox остается чистым для новых задач
- Финальные файлы уже в assets/ с правильными именами

### Проверка

После копирования и очистки проверь:
```bash
ls assets/ | grep eve_        # Файлы в assets
ls inbox/                     # inbox должен быть пуст (или почти пуст)
```

---

## Шаг 6: Создание скрипта

### Шаблон скрипта

```python
"""
Автоматизация: [Краткое описание]

Описание:
    [Подробное описание что делает скрипт]

Процесс:
    1. [Шаг 1]
    2. [Шаг 2]
    ...

UI элементы:
    - eve_element1.png - [описание]
    - eve_element2.png - [описание]

Автор: Claude Code
Дата: YYYY-MM-DD
"""

import sys
import os
import time
import random
import logging
from typing import Optional

# Добавляем путь к eve фреймворку
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from eve import (
    activate_window,
    click_on_image,
    find_image,
    wait_image,
    wait_image_disappear,
    type_text,
    press_key,
    hotkey,
    screenshot
)
from eve.mouse import move_to, click

# Настройка логирования
logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s [%(levelname)s] %(message)s',
    handlers=[
        logging.FileHandler('logs/eve_bot.log', encoding='utf-8'),
        logging.StreamHandler()
    ]
)
logger = logging.getLogger(__name__)


def humanized_delay(min_sec: float = 0.3, max_sec: float = 0.8):
    """Человекоподобная задержка."""
    delay = random.uniform(min_sec, max_sec)
    time.sleep(delay)


def main_task():
    """
    Главная функция выполнения задачи.

    Returns:
        bool: True если успешно, False при ошибке
    """
    logger.info("=" * 50)
    logger.info("Запуск автоматизации EVE Online")
    logger.info("=" * 50)

    # Шаг 1: Активация окна EVE
    logger.info("Шаг 1: Активация окна EVE Online")
    if not activate_window("EVE"):
        logger.error("Окно EVE не найдено")
        return False

    humanized_delay(0.5, 1.0)

    # Шаг 2: Открыть Probe Scanner
    logger.info("Шаг 2: Открытие Probe Scanner")
    if not click_on_image("eve_probe_scanner_button.png", timeout=10):
        logger.error("Кнопка Probe Scanner не найдена")
        return False

    humanized_delay()

    # Шаг 3: Найти аномалию
    logger.info("Шаг 3: Поиск аномалии")
    anomaly_coords = wait_image("eve_anomaly_small_cluster.png", timeout=15)
    if not anomaly_coords:
        logger.error("Аномалия не найдена в списке")
        return False

    # Кликнуть по аномалии с хуманизацией
    x, y = anomaly_coords
    move_to(x, y, duration=random.uniform(0.3, 0.7))
    humanized_delay(0.1, 0.3)
    click(x, y)

    logger.info("Аномалия выбрана")
    humanized_delay()

    # Шаг 4: Warp To
    logger.info("Шаг 4: Варп к аномалии")
    if not click_on_image("eve_warp_to_button.png", timeout=5):
        logger.error("Кнопка Warp To не найдена")
        return False

    # Ждем завершения варпа
    logger.info("Ожидание завершения варпа...")
    time.sleep(random.uniform(10, 15))  # Примерное время варпа

    # ... остальные шаги ...

    logger.info("=" * 50)
    logger.info("Автоматизация завершена успешно")
    logger.info("=" * 50)
    return True


if __name__ == "__main__":
    try:
        # Создаем папку для логов если нет
        os.makedirs("logs", exist_ok=True)

        # Запускаем основную задачу
        success = main_task()

        if success:
            logger.info("Скрипт завершен успешно")
            sys.exit(0)
        else:
            logger.error("Скрипт завершен с ошибками")
            sys.exit(1)

    except KeyboardInterrupt:
        logger.warning("Прервано пользователем (Ctrl+C)")
        sys.exit(130)
    except Exception as e:
        logger.exception(f"Критическая ошибка: {e}")
        sys.exit(1)
```

### Ключевые требования к скрипту

✅ **ОБЯЗАТЕЛЬНО:**
- Подробное логирование каждого шага
- Обработка ошибок (try/except)
- Таймауты для всех wait_image()
- Человекоподобные задержки
- Плавные движения мыши
- Комментарии на русском
- Docstrings для функций

❌ **ЗАПРЕЩЕНО:**
- Фиксированные задержки без рандомизации
- Телепортация курсора (click без move_to)
- Молчаливые ошибки (без логирования)
- Хардкод координат (только через find_image)

---

## Шаг 7: Тестирование

### Как тестировать

1. **Попроси пользователя запустить:**
   ```bash
   python scripts/eve_mining_automation.py
   ```

2. **Наблюдай логи:**
   - Проверяй что все шаги выполняются
   - Ищи ошибки и варнинги
   - Анализируй причины сбоев

3. **Итерация:**
   - Если элемент не найден - проверь изображение
   - Если таймаут - увеличь время ожидания
   - Если телепортация курсора - добавь move_to

### Типичные проблемы

**Проблема**: `ERROR: Элемент не найден`
**Решение**:
- Проверь правильность имени файла
- Убедись что элемент видим на экране
- Попроси пользователя сделать новый скриншот
- Уменьши confidence в find_image()

**Проблема**: Бот слишком быстрый
**Решение**:
- Добавь humanized_delay() между действиями
- Увеличь duration в move_to()
- Добавь рандомизацию

**Проблема**: Бот пропускает элементы
**Решение**:
- Увеличь timeout в wait_image()
- Добавь дополнительные проверки
- Улучши логирование

---

## Советы и best practices

### Работа с изображениями

1. **Делай скриншоты точно** - вырезай только нужный элемент
2. **Разные состояния** - если элемент меняется, нужны разные скриншоты
3. **Уникальность** - элемент должен быть уникален на экране
4. **Размер** - не слишком маленькие (минимум 20x20px)

### Структура кода

1. **Одна функция = одна задача**
2. **Логируй каждый шаг**
3. **Обрабатывай ошибки**
4. **Комментируй сложные места**

### Хуманизация

1. **Всегда** используй move_to() перед click()
2. **Всегда** рандомизируй задержки
3. **Избегай** идеальных паттернов
4. **Добавляй** естественную вариативность

---

## Checklist перед завершением

Перед тем как считать задачу выполненной:

- [ ] Все скриншоты из inbox/ обработаны
- [ ] Все файлы переименованы правильно и в assets/
- [ ] Скрипт содержит подробное логирование
- [ ] Все действия хуманизированы (move_to + delays)
- [ ] Обработаны все возможные ошибки
- [ ] Добавлены таймауты
- [ ] Код прокомментирован
- [ ] Скрипт протестирован (или готов к тесту)
- [ ] Логи читаемы и информативны

---

## Пример полного процесса

### Задача
```
Пользователь: "Автоматизируй открытие инвентаря и переброс items в контейнер"
```

### Выполнение

1. **Claude**:
   ```
   Понял. Нужны скриншоты:
   - Кнопка Inventory
   - Item в инвентаре
   - Контейнер (куда перебрасывать)
   Заскриншоть и положи в inbox/
   ```

2. **Пользователь**: *Делает 3 скриншота → inbox/*

3. **Claude**:
   ```bash
   ls inbox/
   # Screenshot_42.png, Screenshot_43.png, Screenshot_44.png
   ```
   ```python
   Read("inbox/Screenshot_42.png")  # Вижу кнопку Inventory
   Read("inbox/Screenshot_43.png")  # Вижу иконку Veldspar Ore
   Read("inbox/Screenshot_44.png")  # Вижу контейнер
   ```
   ```
   Вижу 3 файла. Это кнопка Inventory, руда Veldspar, и контейнер?
   ```

4. **Пользователь**: "Да, верно"

5. **Claude**:
   ```bash
   # Копируем в assets
   cp inbox/Screenshot_42.png assets/eve_inventory_button.png
   cp inbox/Screenshot_43.png assets/eve_item_veldspar_ore.png
   cp inbox/Screenshot_44.png assets/eve_container.png

   # Чистим inbox
   rm inbox/Screenshot_42.png
   rm inbox/Screenshot_43.png
   rm inbox/Screenshot_44.png
   ```

6. **Claude**: *Пишет скрипт `scripts/eve_transfer_items.py`*

7. **Claude**: "Скрипт готов. Запусти `python scripts/eve_transfer_items.py` для теста"

8. **Пользователь**: *Тестирует*

9. **Итерация** если нужно

---

**Следуй этому процессу и всё будет работать эффективно!** 🚀
